# RES-21-DR-001 — PerformanceScience-Eval V1 benchmark design freeze

## Status and scope

| Field | Frozen value |
| --- | --- |
| Mission | `RES-21-BENCHMARK-DESIGN-001` |
| Linear issue | `RES-21` — `P4 — Build and Freeze PerformanceScience-Eval V1` |
| Design status | `FROZEN` |
| Benchmark status | Design contract only; no V1 dataset is generated by this mission |
| Benchmark semantic version | `PerformanceScience-Eval@1.0.0` |
| Case schema version | `pse-case-schema@1.0.0` |
| Qualified base | `74c9e3d05f2da41facc25b7aa0e880d5668cc055` |
| Scientific-engine behavior | Unchanged |
| Model inference/training | Not implemented and not run |

This record freezes the contract that the next RES-21 implementation mission
must realize. It does not create the complete benchmark, an evaluation
runner, a model runtime, training data, or model results.

The design is bounded by the sealed reasoning/claims/refusal architecture,
the current V2 scientific constitution, the measurement/provenance contract,
and the qualified RES-71 deterministic engine. It is an evaluation contract,
not a second scientific engine.

## Entry gate

The design was inspected at the required entry state:

| Gate | Result |
| --- | --- |
| One worktree | `PASS` — `/home/litju/projects/DynamisLM` is the only worktree |
| Branch | `PASS` — `work/res-21-performance-science-eval-v1` |
| `HEAD` | `PASS` — `74c9e3d05f2da41facc25b7aa0e880d5668cc055` |
| Worktree | `PASS` — clean before mutation |
| Local `origin/main` | `PASS` — `74c9e3d05f2da41facc25b7aa0e880d5668cc055` |
| Remote `origin/main` | `PASS` — remote `refs/heads/main` resolves to the same SHA |
| Linear `RES-21` | `In Progress` |
| Linear `RES-71` | `Done` |

The RES-71 receipt's historical base fields are retained as upstream
qualification evidence. The current mission base and all V1 artifacts are
bound to the qualified repository `HEAD` above.

## Authority inspected

The following are normative inputs to this freeze:

- [`REASONING_CLAIMS_EVALUATION_V1.md`](../architecture/REASONING_CLAIMS_EVALUATION_V1.md)
  for capability claims, claim levels, practitioner question classes, error
  asymmetry, and refusal semantics.
- [`SCIENTIFIC_CONSTITUTION_V2.md`](../architecture/SCIENTIFIC_CONSTITUTION_V2.md)
  for population, evidence, measurement identity, deterministic authority,
  claim, causal, and refusal boundaries.
- [`MEASUREMENT_DATA_PROVENANCE_V1.md`](../architecture/MEASUREMENT_DATA_PROVENANCE_V1.md)
  for identity, lineage, comparability states, and reprocessing invariants.
- RES-71 qualification artifacts:
  [`RES71-SCIENTIFIC-ENGINE-QUALIFICATION.md`](../qualification/RES71-SCIENTIFIC-ENGINE-QUALIFICATION.md),
  [`RES71-OPERATION-INVENTORY.md`](../qualification/RES71-OPERATION-INVENTORY.md),
  [`RES71-UNRESOLVED-COMPUTATIONS.md`](../qualification/RES71-UNRESOLVED-COMPUTATIONS.md),
  [`RES71-COVERAGE-MATRIX.md`](../qualification/RES71-COVERAGE-MATRIX.md),
  [`RES71-PROVENANCE-SERIALIZATION.md`](../qualification/RES71-PROVENANCE-SERIALIZATION.md),
  [`RES71-VERIFIER-REFERENCE-INTERFACE.md`](../qualification/RES71-VERIFIER-REFERENCE-INTERFACE.md),
  and [`RES71-GATE-RECEIPT.json`](../qualification/RES71-GATE-RECEIPT.json).
- RES-60, RES-62, RES-69, and RES-70 decision records for population/source
  authority, longitudinal provenance, statistical boundaries, comparability,
  analysis authorization, evidence applicability, and claim authority.
- The live Linear descriptions and status of RES-21 and RES-71.

## 1. Normative vocabulary and authority boundary

### 1.1 V1 claim boundary

PerformanceScience-Eval V1 evaluates whether a model can select and express a
scientifically authorized interpretation of supplied language, typed context,
evidence, and deterministic results. The V1 capability claim is:

> Given practitioner language, structured measurement/provenance context,
> applicable evidence, and registered deterministic results, the model can
> resolve identity, protocol, comparability, analysis level, claim level,
> evidence scope, interpretation, error class, or justified refusal without
> inventing numerical science or overclaiming causality.

V1 does not claim raw-video understanding, unrestricted numerical computation,
clinical diagnosis, fatigue/readiness/injury prediction, population
generalization from noncanonical evidence, causal identification from routine
monitoring, or any operation that RES-71 marks deferred, represented-only, or
rejected.

### 1.2 Authority precedence

When two representations disagree, the following precedence is mandatory:

1. **RES-71 deterministic authority** wins for numerical/verifiable values,
   registered operation identity, units, tolerance, deterministic refusal,
   comparability output, and claim-authority output. The benchmark calls the
   qualified engine/reference interface; it does not reimplement equations.
2. **The registered RES-70/RES-69/RES-60/RES-62 authority** wins for typed
   semantic states, analysis prerequisites, evidence applicability, population
   scope, refusal class/reason codes, and claim-level boundaries.
3. **The source document and exact evidence span** win for source-backed
   extraction facts. Source identity, version, content digest, and locator are
   required.
4. **The frozen expert-authored rubric** is the authority for semantic cases
   that are not mechanically derivable, but it must cite explicit architecture,
   decision-record, operation, or source authority references.

No LM-generated answer, model output, model-as-judge decision, or post hoc
human preference can become V1 ground truth. A semantic case without a
structured, reviewable expert answer and explicit authority references is not
eligible for a frozen manifest.

### 1.3 Claim and comparability vocabularies

The case contract uses the existing vocabularies, not new aliases:

- Measurement claim levels: `OBSERVED_VALUE`, `NUMERICAL_CHANGE`,
  `COMPARABLE_CHANGE`, `CHANGE_RELATIVE_TO_MEASUREMENT_ERROR`, and
  `PRACTICAL_OR_DECISION_MEANINGFULNESS`.
- Relationship claim levels: `OBSERVATION`, `DESCRIPTIVE_CHANGE`,
  `ASSOCIATION`, `TEMPORAL_ASSOCIATION`, `MECHANISTIC_HYPOTHESIS`, and
  `CAUSAL_EVIDENCE`. Prediction remains orthogonal to causal level.
- Comparability states: `COMPARABLE`, `COMPARABLE_WITH_CONDITIONS`,
  `REQUIRES_TRANSFORMATION`, `BRIDGE_VALIDATION_REQUIRED`, `NOT_COMPARABLE`,
  and `INSUFFICIENT_INFORMATION`.
- Refusal classes: `IDENTITY_UNRESOLVED`, `POPULATION_SCOPE_UNRESOLVED`,
  `CANONICAL_ELIGIBILITY_FAILED`, `COMPARABILITY_UNESTABLISHED`,
  `EVIDENCE_SCOPE_UNSUPPORTED`, `DATA_ADEQUACY_INSUFFICIENT`,
  `ANALYSIS_DESIGN_MISMATCH`, `UNCERTAINTY_LIMITS_CLAIM`,
  `CAUSAL_IDENTIFICATION_UNSUPPORTED`, and `COMPUTATION_NOT_REGISTERED`.

The benchmark may use a case-local answer label such as
`NOT_REQUESTED` for a non-comparison field. It must not invent a new
scientific comparability or refusal state.

## 2. Immutable V1 case contract

### 2.1 Canonical case shape

Every case is an immutable, canonical record. Repeated fields are ordered
tuples; maps are canonicalized by key; authority-bearing identifiers are typed
or stable strings; all hashes use the repository's canonical serialization
convention. The next mission must implement this shape without allowing
caller-supplied formulas or authority overrides.

```text
BenchmarkCaseV1 {
    benchmark_version: "PerformanceScience-Eval@1.0.0"
    schema_version: "pse-case-schema@1.0.0"
    case_id: stable globally unique string
    case_version: semantic version
    capability_id: C01..C18
    benchmark_family: F01..F14
    practitioner_question_class: one of the eight frozen classes
    question: canonical prompt text
    input: InputContract
    source_evidence_refs: tuple[EvidenceReference, ...]
    expected_answer: ExpectedStructuredAnswer
    authority: tuple[AuthorityBinding, ...]
    refusal_expectation: RefusalExpectation
    claim_contract: ClaimContract
    comparability_contract: ComparabilityContract
    scoring_contract: ScoringContract
    tolerance_contract: ToleranceContract | None
    provenance: CaseProvenance
    split: SplitBinding
    contamination: ContaminationBinding
    difficulty: DifficultyBinding
    adversarial_tags: tuple[string, ...]
    case_payload_hash: sha256
}
```

`case_id` is never reused. A semantic mutation creates a new case ID or a new
case version and a new hash; it never edits an existing frozen case. The
`case_payload_hash` is computed over the canonical case payload before the
manifest and split digests are attached.

### 2.2 Field-level contract

| Field | Required semantics and invariants |
| --- | --- |
| `benchmark_version`, `schema_version` | Exact registered strings. A case cannot silently move between benchmark or schema versions. |
| `case_id`, `case_version` | Stable identity plus semantic version. The ID is not a content hash; the hash detects mutation. |
| `capability_id`, `benchmark_family` | Both are controlled vocabulary values. The family is the evaluation surface; the capability is the observable claim. |
| `practitioner_question_class` | Exactly one of the eight classes in Section 4.3. Multi-intent prompts are split into cases unless the rubric explicitly declares a structured multi-intent task. |
| `question` | The model-visible question. It contains no hidden answer, authority digest that would leak the answer, or unregistered scientific instruction. |
| `input.modality` | One or more of `TEXT`, `TABLE`, `STRUCTURED_MEASUREMENT_RECORD`, `EVIDENCE_EXCERPT`, and `COMPOSITE`. Raw media is outside V1. |
| `input.structured_context` | Canonical typed context: identities, units, protocol fields, observation/result references, analysis support, context, missingness, and deterministic-result references. Free text cannot override typed fields. |
| `source_evidence_refs` | Tuple of source, authority, engine, rubric, or exact evidence-span references. Each reference carries a content/registry digest, locator, scope, and applicability. Real empirical bytes remain outside Git. |
| `expected_answer` | A discriminated structured answer, never only a prose string. It contains required field IDs, expected values/labels/sets, accepted registered aliases, prohibited claims, and safe lower-level descriptions where applicable. |
| `authority` | One or more explicit `AuthorityBinding` records. Each states the authority kind, source/reference ID, version, digest, and the fields it governs. Conflicts invalidate the case for review. |
| `refusal_expectation` | Whether refusal is required, allowed, or prohibited; expected refusal class; required reason codes; missing-information fields; blocked claim; and safe descriptions. |
| `claim_contract` | Requested claim/estimand, maximum supported claim level, lower-level claims that remain safe, and prohibited escalation. Claim levels are axis-specific. |
| `comparability_contract` | Claim-relative requested state, material dimensions, dimension findings, conditions, transformations, and whether comparison is not applicable. Pairwise comparability is never inferred transitively. |
| `scoring_contract` | Versioned scorer profile, required output fields, critical fields, accepted normalization, error-class rules, and task-level status policy. No global case weight is embedded. |
| `tolerance_contract` | Required only for numeric fields. Contains units, absolute tolerance, relative tolerance, finite-value rule, and comparison rule. A tolerance is not a license to change the registered engine output. |
| `provenance` | Mutually explicit origin class, authority lineage, creator/reviewer references, generator and seed metadata when applicable, parent case hash for mutations, source/artifact hashes, and derivation status. |
| `split` | Immutable split name, split-manifest version/hash, allocation stratum, isolation cluster, and membership digest. |
| `contamination` | Source/document/DOI IDs, text fingerprints, semantic-cluster ID, generator namespace/seed block, benchmark artifact IDs, and future training-exclusion identifiers. |
| `difficulty`, `adversarial_tags` | Registered difficulty level plus one or more controlled adversarial tags. These describe case construction and do not change authority or scoring. |
| `case_payload_hash` | Canonical SHA-256 digest. A mismatch is a hard validation failure. |

### 2.3 Input contract

The model-visible input is a canonical envelope:

```text
InputContract {
    modality: tuple[InputModality, ...]
    question_text: string
    structured_context: canonical object
    observations: tuple[ObservationView, ...]
    deterministic_results: tuple[DeterministicResultView, ...]
    evidence_excerpts: tuple[EvidenceExcerpt, ...]
    missing_information: tuple[string, ...]
}
```

`ObservationView` preserves the separation
`ObservationContext + MeasurementIdentity + MeasurementResult + Provenance`.
It may expose a source/provider-derived value, but it never relabels it as a
DynamisLM direct measurement. `DeterministicResultView` includes the operation
identity, method version, output unit, result/provenance hash, authority
reference, and any registered refusal. The model may be asked to interpret a
result; it is not asked to recompute it.

### 2.4 Expected structured answer

`ExpectedStructuredAnswer` is a typed union with these outcome forms:

- `ANSWER`: required semantic labels, extracted fields, normalized identities,
  analysis class, claim level, and bounded interpretation.
- `NUMERIC_RESULT`: the exact registered result reference and expected output
  field(s), with the engine-owned tolerance contract.
- `COMPARABILITY_DECISION`: exact state, material dimension findings,
  conditions, required transformations, and authority reference.
- `CLAIM_AUTHORITY_DECISION`: authorized/refused/partially authorized state,
  allowed lower-level description, and refusal data where present.
- `EVIDENCE_EXTRACTION`: exact source span or canonical source field, claim
  scope, population/evidence applicability, and unsupported inference fields.
- `REFUSAL`: the structured refusal result described below.

All semantic alternatives are explicit in the case rubric. A free-form answer
that happens to sound scientifically plausible is not a match unless its
required structured fields match. Optional explanation is retained for error
analysis only and is never scored by an LM judge.

### 2.5 Refusal contract

For a refusal-expected case, the authoritative answer contains:

```text
RefusalExpectation {
    decision: REQUIRED | ALLOWED | PROHIBITED
    blocked_claim: exact claim text or claim reference
    refusal_class: RefusalClass
    reason_codes: tuple[string, ...]
    missing_information: tuple[string, ...]
    what_can_still_be_safely_described: tuple[string, ...]
    safe_lower_claim_level: optional claim level
}
```

The next mission must compare against the existing RES-71/RES-70 refusal
taxonomy. It must not collapse all refusals into `INSUFFICIENT_INFORMATION`.
A refusal blocks the requested claim, not necessarily the underlying
observation. A correct refusal therefore includes both the blocked claim and
the strongest independently safe description.

### 2.6 Case invariants

The following are hard validation failures:

1. missing authority reference, source digest, or reviewer binding;
2. numeric expected values without a registered operation/reference and
   tolerance;
3. an operation, transformation, claim level, comparability state, or refusal
   code not present in the current authority;
4. source-backed evidence paired with an unlabelled synthetic gold value;
5. an expected answer that requires the model to supply a deterministic
   calculation owned by Python;
6. a hidden case whose payload, answer, or source bytes are available to a
   training/data-pipeline principal;
7. a case assigned to more than one split or an isolation cluster split across
   incompatible partitions;
8. a changed case hash after manifest freeze; or
9. a semantic rubric that cannot be represented as deterministic structured
   fields and explicit prohibited claims.

## 3 Frozen benchmark families and observable capabilities

### 3.1 Capability IDs

The V1 model capability set is deliberately finite and observable:

| ID | Observable capability |
| --- | --- |
| `C01` | Terminology and construct identification: resolve practitioner language to a registered test, construct, or exposure. |
| `C02` | Protocol extraction: recover material protocol, device, event, phase, threshold, trial-selection, aggregation, and context fields. |
| `C03` | Measurement identity: distinguish metric, measurand, metric definition, estimator, method/version, and identity from display label. |
| `C04` | Value-origin and scientific-role classification: distinguish direct, source-reported, provider-derived, DynamisLM-derived, mechanical-derived, model-estimated, latent, and physiological claims. |
| `C05` | Unit and normalization reasoning: identify exact-unit requirements, normalization, denominator, scale, and conversion-versus-harmonization boundaries. |
| `C06` | Frame, sign, event, and phase reasoning: resolve reference frame, sign convention, timebase, event/phase boundaries, and estimator prerequisites. |
| `C07` | Device/protocol/method comparability: select claim-relative comparability state and material dimension findings, without correlation or label shortcuts. |
| `C08` | Biomechanics calculation interpretation: consume registered deterministic outputs and identify when a requested biomechanics quantity is unregistered or invalid. |
| `C09` | Longitudinal change reasoning: distinguish observation, numerical change, comparable change, error-relative change, and practical meaning. |
| `C10` | Analysis-class selection: choose or refuse the registered analysis class from support shape, identity, design, and prerequisites. |
| `C11` | Within/between/group/multilevel reasoning: identify the requested estimand and reject level substitution or pseudoreplication. |
| `C12` | Reliability, uncertainty, and validity interpretation: distinguish random error, reliability, agreement, uncertainty, validity, and meaningfulness. |
| `C13` | Evidence extraction: extract supported facts and exact spans from source-backed material with provenance. |
| `C14` | Evidence applicability and scope: distinguish canonical target evidence, direct target evidence, indirect measurement evidence, context-only evidence, and unresolved scope. |
| `C15` | Causal-language discipline: classify observation, association, temporal association, mechanism hypothesis, prediction, and causal evidence. |
| `C16` | Deterministic-result interpretation: translate a typed result/refusal into a bounded answer without changing its numerical or provenance authority. |
| `C17` | Scientific error detection: detect the frozen critical/high error classes and state the scientifically correct boundary. |
| `C18` | Justified refusal: recognize missing identity, metadata, comparability, support, evidence, uncertainty, causal design, or registered computation and issue a specific refusal. |

### 3.2 RES-21 family coverage map

Every required RES-21 family maps to at least one capability and has both
answerable and adversarial/refusal cases in the later manifest.

| Family | Required observable coverage |
| --- | --- |
| `F01 terminology/construct identification` | `C01`, `C03`, `C14`; aliases do not mint identity. |
| `F02 protocol extraction` | `C02`, `C06`, `C18`; missing event/device/threshold fields are explicit. |
| `F03 metric/measurand definition and normalization` | `C03`, `C04`, `C05`; same label/different measurand and provider-origin cases are required. |
| `F04 device/protocol comparability` | `C07`, `C05`, `C14`, `C18`; all six comparability states are eligible where the registered authority supports them. |
| `F05 biomechanics calculations` | `C08`, `C03`, `C05`, `C06`, `C16`; numerical cases consume RES-71 references and deferred quantities refuse. |
| `F06 unit/frame reasoning` | `C05`, `C06`, `C07`, `C08`; unit conversion is separated from method/frame harmonization. |
| `F07 longitudinal-change reasoning` | `C09`, `C12`, `C15`, `C18`; the claim ladder is scored explicitly. |
| `F08 statistical-analysis selection` | `C10`, `C11`, `C12`, `C18`; operation disposition and prerequisites are part of the answer. |
| `F09 within-athlete vs between-athlete reasoning` | `C10`, `C11`, `C15`, `C18`; one-row-per-athlete support cannot answer a within-athlete question. |
| `F10 uncertainty/reliability/validity interpretation` | `C12`, `C14`, `C16`, `C18`; no reliability/agreement/meaningfulness conflation. |
| `F11 evidence extraction` | `C13`, `C14`, `C03`, `C18`; exact span, source scope, applicability, and unsupported inference are separate fields. |
| `F12 causal-language discipline` | `C15`, `C09`, `C14`, `C18`; causal escalation must refuse when identification is absent. |
| `F13 scientific error detection` | `C17` plus all affected capabilities; a case may score multiple explicit error classes. |
| `F14 scientific refusal/insufficient-information behavior` | `C18` plus every identity, comparability, analysis, evidence, uncertainty, and causal boundary. |

### 3.3 RES-71 authority-surface map

The benchmark consumes the following qualified surfaces; it does not duplicate
their science:

| RES-71 surface | Authority consumed by V1 | Case use |
| --- | --- | --- |
| Population/source authority | RES-60/63 population, source, origin, granularity, quarantine, and evidence applicability decisions | Scope, canonical-target, indirect-evidence, and refusal cases. |
| Football world/context | RES-61/62 context, match/training relation, longitudinal record, and temporal identity | Context extraction, longitudinal support, and context-mismatch cases. |
| Ingestion/record/provenance | RES-62/63 raw artifact, canonical row, processing run, lineage, source hash, and reprocessing identity | Provenance extraction, source identity, and tamper/error cases. |
| External load/GNSS/optical | RES-64 registered identities and qualified/deferred metric operations | Unit, threshold, provider-derived, normalization, and refusal cases. |
| CMJ | RES-34–50 and RES-65 event/phase/estimator/force/power identities and registered operations | Ballistic result interpretation, event/frame identity, and deferred RFD/COM cases. |
| Strength/IMTP/VBT | RES-66 registered strength identities and deferred MPV/estimated-1RM boundaries | Direct/derived/model-estimate and unregistered-operation cases. |
| Sprint/max velocity/COD/RSA/30–15 IFT | RES-67 field-testing identities and refusals | RSA calculation, threshold/method identity, VIFT relabelling, and deferred acceleration cases. |
| DJ/bench-throw/medicine-ball throw | RES-68 family identities and explicit non-computation paths | Protocol/measurand distinction and generic-power refusal cases. |
| Longitudinal statistics/reliability/error | RES-69 operation inventory, scale/reliability authority, support, and deferred analysis boundaries | Change, error, agreement, reliability, scale, and meaningfulness cases. |
| Cross-source comparability | RES-70 pairwise state, material dimensions, bridge registry, conditions, registry hashes, and non-transitivity | Same-label, device/protocol, bridge, method-version, and source-context cases. |
| Analysis capability | RES-70 analysis capability registry, level identity, prerequisites, authorization/refusal | Analysis-class, support-shape, within/between, and pseudoreplication cases. |
| Claim authority | RES-70 two-axis claim authority, evidence applicability, lower-level safe descriptions, and refusal results | Claim ladder, causal boundary, evidence scope, and refusal cases. |

Cross-cutting RES-71 provenance/serialization authority requires V3-compatible
canonical hashes and complete lineage on every deterministic result. It does
not authorize a second scoring calculation.

## 4 Practitioner question classes

The eight question classes are frozen from the reasoning architecture. Each
case has exactly one primary class; secondary intents are structured fields,
not an excuse to omit the primary contract.

| Class | Required family/capability coverage | RES authority surfaces |
| --- | --- | --- |
| `MEASUREMENT_IDENTITY_AND_PROVENANCE` | `F01`, `F02`, `F03`, `F11`, `F14`; `C01–C04`, `C13`, `C18` | Population/source, ingestion/record/provenance, relevant measurement family, RES-71 inventory. |
| `COMPARABILITY_AND_HARMONIZATION` | `F04`, `F06`, `F13`, `F14`; `C05–C07`, `C14`, `C17`, `C18` | Family comparators, RES-70 comparability/bridge authority, provenance. |
| `INDIVIDUAL_LONGITUDINAL_CHANGE` | `F07`, `F10`, `F12`, `F14`; `C09`, `C12`, `C15`, `C18` | RES-62 record, RES-69 statistics/error, RES-70 claim authority. |
| `GROUP_OR_SQUAD_CHANGE` | `F07`, `F08`, `F09`, `F11`, `F12`; `C09–C15` | Population/context, RES-62 support, RES-69 level rules, RES-70 analysis/evidence/claim authority. |
| `RELATIONSHIP_AND_MULTILEVEL_STRUCTURE` | `F08`, `F09`, `F10`, `F12`, `F13`; `C10–C12`, `C15`, `C17`, `C18` | RES-69 and RES-70 analysis capability and claim authority. |
| `CONSTRUCT_AND_CLAIM_INTERPRETATION` | `F01`, `F03`, `F07`, `F10`, `F12`, `F13`; `C01`, `C03`, `C09`, `C12`, `C14–C17` | Scientific constitution, measurement families, evidence applicability, claim authority. |
| `METHOD_AND_ANALYSIS_REASONING` | `F02`, `F04`, `F05`, `F06`, `F08`, `F09`; `C02`, `C05–C12`, `C18` | Operation inventory, family identities, RES-70 analysis/comparability authority. |
| `ANSWERABILITY_AND_REFUSAL` | all families; all capabilities, with `C18` primary | RES-71 unresolved inventory, refusal taxonomy, RES-70 safe fallback, claim authority. |

## 5 Error taxonomy and error asymmetry

Error classes are scored as explicit events. They are not collapsed into a
single aggregate score, and multiple classes may be recorded for one case.

### 5.1 Critical errors

The following are V1 critical and each receives its own rate, numerator,
denominator, examples, and split breakdown:

- `FALSE_SCIENTIFIC_ACCEPTANCE`: accepts a claim that the authority requires
  refusing or qualifies only at a lower level.
- `INVENTED_NUMERICAL_SCIENCE`: supplies, changes, or derives authoritative
  numerical science without the registered result/operation authority.
- `FALSE_COMPARABILITY_ACCEPTANCE`: declares a comparison supported when the
  claim-relative authority says otherwise or omits a material mismatch.
- `CAUSAL_OVERCLAIM`: asserts causal evidence/effect from support that does not
  satisfy the causal authority contract.
- `BETWEEN_TO_WITHIN_MISINFERENCE`: treats between-athlete information as a
  within-athlete longitudinal result.

### 5.2 High-severity errors

- `WRONG_MEASUREMENT_IDENTITY`: wrong construct, measurand, metric definition,
  method, protocol, or estimator.
- `WRONG_ANALYSIS_CLASS`: selects an analysis class whose registered
  prerequisites or estimand do not match the question.
- `DIRECT_DERIVED_COLLAPSE`: presents source/provider/DynamisLM-derived or
  mechanical-derived output as a direct measurement.
- `UNSUPPORTED_LATENT_OR_PHYSIOLOGICAL_INFERENCE`: infers fatigue, readiness,
  injury, physiology, or another latent construct without authority.

### 5.3 Additional measured behaviors

- `OVER_REFUSAL`: refuses an answerable or conditionally answerable claim, or
  refuses more strongly than the frozen authority requires.
- `UNDER_SPECIFIED_REFUSAL`: identifies that a claim is blocked but omits the
  required blocked claim, refusal class, reason code, missing information, or
  safe lower-level description.
- `EXCESSIVE_CONSERVATISM`: returns a lower claim/answer than the authority
  supports, including an unnecessary generic refusal, while not necessarily
  refusing the whole case.

The last three are reported separately. `OVER_REFUSAL` is a decision error;
`UNDER_SPECIFIED_REFUSAL` is a refusal-contract completeness error; and
`EXCESSIVE_CONSERVATISM` is an unjustified loss of supported information.

### 5.4 Error attribution rules

1. A critical error takes precedence over a high-severity label only for the
   case status; all applicable error labels remain recorded.
2. A wrong lower-level answer is not a critical error merely because it is
   conservative; it is `EXCESSIVE_CONSERVATISM` only when the authority
   supports the omitted stronger/conditional field.
3. A refusal on a genuinely blocked case is not over-refusal, even if the
   model cannot provide a safe description; the latter is under-specified.
4. Error rates use the eligible-case denominator for that class and are
   reported by family, capability, question class, origin, difficulty, and
   split.

## 6 Case origins and provenance

`origin_class` is required and is never inferred from the presence of a
numeric value. The five primary classes are:

| Origin class | Frozen contract |
| --- | --- |
| `DETERMINISTIC_SYNTHETIC` | Inputs are generated from a versioned pure generator and disjoint seed namespace. The typed case is clearly synthetic; any numeric answer still needs a registered operation or frozen semantic authority. |
| `DETERMINISTIC_ENGINE_DERIVED` | Input is bound to a RES-71 operation/reference case and its exact engine/reference output, operation version, tolerance, registry/manifest digest, and full provenance. No duplicate formula is stored as an independent gold. |
| `EXPERT_AUTHORED_SEMANTIC` | An expert author supplies the structured answer/rubric and explicit authority references; an independent expert reviewer approves it. The rubric is versioned and hash-bound. |
| `SOURCE_BACKED_EVIDENCE_EXTRACTION` | Ground truth is tied to source ID, source/artifact/content hash, DOI or stable locator, exact excerpt/span, extraction rule, scope, and applicability. Source bytes remain outside Git unless separately authorized. |
| `ADVERSARIAL_MUTATION` | A deterministic mutation of a parent case. It records parent case hash, mutation operator/version, seed, changed fields, and revalidated authority. The parent’s primary source authority is preserved; mutation metadata never relabels source-backed truth as synthetic. |

Composite cases use one origin record per expected-answer component. A
source-backed component and a synthetic component may coexist only when the
lineage, authority, and scorer for each component are explicit. Unlabelled
mixing is invalid.

Every case also records:

- author, reviewer, approval timestamp, and rubric/reference digest;
- source/artifact IDs and content digests where applicable;
- generator ID/version and non-overlapping seed namespace where applicable;
- parent case/mutation identifiers where applicable;
- evidence applicability and population scope;
- derivation graph from input observations to result/reference;
- serialization version and case payload hash.

## 7 Split governance

### 7.1 Splits

V1 has exactly three mutually exclusive splits:

1. `PUBLIC_DEVELOPMENT`: model-development and pipeline-debugging cases. The
   case payload and structured answer may be distributed, but all benchmark
   artifacts remain in the training-exclusion registry.
2. `FROZEN_VALIDATION`: read-only validation cases. They are available to the
   evaluation workflow after freeze but are not available to training or
   corpus-construction workflows.
3. `HIDDEN_FINAL`: final cases and authoritative answers. Payload and answer
   material are stored in an evaluation-only namespace and are not exposed to
   training, data, or model-development principals.

The initial allocation target is `60% / 20% / 20%` by case count after
stratified allocation. The exact counts are not guessed in this design; the
frozen split manifest records them and validation fails if the allocation or
coverage constraints below cannot be met.

### 7.2 Deterministic allocation and balancing

Allocation is performed once, after case validation and contamination audit:

1. Build an isolation cluster from source family/document, construct/test
   identity, protocol/template, expert-author batch, generator family, and
   mutation lineage.
2. Assign the entire cluster atomically; no cluster may cross public,
   validation, and hidden partitions.
3. Within the eligible clusters, use a versioned deterministic hash of
   `benchmark_version + isolation_cluster_id + origin_class + seed_block` to
   produce the target allocation, then solve the registered balancing
   constraints in canonical cluster order.
4. Balance each split across all 14 families, all 18 capabilities, all eight
   question classes, answer/refusal outcome, origin class, difficulty, and
   critical-error eligibility. If a required cell cannot be filled without
   leakage, generation stops with `STATUS=BLOCKED`.
5. Each critical error class has eligible cases in validation and hidden
   splits; each family and capability has answerable and boundary/refusal
   coverage in validation and hidden splits where the capability applies.
6. Freeze membership in `split-manifest@1.0.0`; changing membership creates a
   new benchmark version and invalidates prior comparisons.

### 7.3 Access and isolation controls

`HIDDEN_FINAL` is architecturally inaccessible to later training workflows:

- hidden payloads and expected answers live in a separate versioned store and
  credential namespace;
- only the evaluation service principal can resolve hidden payloads/answers;
- training/data-pipeline principals receive a deny response for hidden case
  IDs and cannot mount the hidden store;
- the public manifest exposes only hidden IDs, split membership commitment,
  case hashes, and exclusion fingerprints, not hidden text or answers;
- evaluation logs store candidate outputs and case hashes, not hidden answer
  payloads in training-readable locations; and
- a preflight check fails closed if a training job requests a hidden artifact
  or if the exclusion registry is unavailable.

No validation or hidden case may be used as training data, preference data,
prompt data, synthetic expansion input, or model-selection tuning data after
freeze.

## 8 Scoring contract

### 8.1 Scoring profiles

Every case references exactly one versioned profile, with optional secondary
diagnostic profiles. Profiles score structured fields and preserve task-level
evidence:

| Profile | Semantics |
| --- | --- |
| `NUMERIC_TOLERANCE_V1` | Compare finite numeric fields in the registered unit using `abs_error <= abs_tol OR relative_error <= rel_tol`; exact unit/field identity is required. |
| `CLASSIFICATION_V1` | Exact controlled-label/set match; report accuracy and macro-F1 per task/family where class support permits. |
| `STRUCTURED_FIELDS_V1` | Score every required field independently, identify missing/wrong/extra fields, and apply case-declared critical-field rules. |
| `EVIDENCE_SPAN_V1` | Exact or registered-normalized source-span/field match plus scope/applicability fields; unsupported inference is an error. |
| `COMPARABILITY_V1` | Exact state match is primary; material dimension findings, reasons, conditions, and bridge/transformation fields are separately scored. |
| `REFUSAL_V1` | Refusal decision, class, reason codes, blocked claim, missing information, and safe description are separately scored. |
| `CAUSAL_BOUNDARY_V1` | Claim level and prohibited causal/latent language are checked against the authority; causal-boundary violations are separately counted. |
| `CALIBRATION_V1` | Optional only for a registered mutually exclusive classification task with a valid probability vector. Report Brier/ECE with a declared sample-size floor; otherwise calibration is `NOT_SCORED`. |

There is no model-as-judge profile. A human expert may review an implementation
dispute, but the frozen ground truth remains the authority binding and the
structured rubric, not the reviewer’s preference.

### 8.2 Numeric and structured semantics

- Numeric fields use the case tolerance, not a benchmark-wide arbitrary
  tolerance. Nonfinite outputs fail.
- A unit mismatch fails even when the number is numerically convertible unless
  the expected answer explicitly contains a registered transformation.
- A classification answer is correct only when the controlled label and
  required qualifiers match. Synonyms are accepted only through a frozen alias
  table bound to the case/rubric.
- Structured-field scoring is unweighted at the global level. Required fields
  are reported as correct, partial, missing, wrong, or extra; critical fields
  can force `FAIL`.
- Evidence answers must identify the source span/field and the scope of the
  claim. A correct phrase with an unsupported generalization is not fully
  correct.

### 8.3 Refusal precision and recall

Refusal metrics are reported at two levels:

1. **Decision level:** expected refusal versus accepted answer. Refusal
   precision is correct refusals divided by all refusals; refusal recall is
   correct refusals divided by all refusal-required cases.
2. **Contract level:** among refusal-required cases, report exact class,
   reason-code, blocked-claim, missing-information, and safe-description
   completeness. This exposes under-specified refusals separately from
   decision-level correctness.

`OVER_REFUSAL`, `UNDER_SPECIFIED_REFUSAL`, and `EXCESSIVE_CONSERVATISM` are
not hidden inside precision/recall; each has its own rate.

### 8.4 Task outcomes

The case outcome is assigned by this precedence:

- `PASS`: an answerable case has every required field correct within the
  registered contract, with no critical/high-severity error.
- `PARTIAL`: the primary decision/claim boundary is correct, but one or more
  noncritical fields are missing, incomplete, or lower-level than authorized.
  An otherwise correct refusal missing required detail is partial and is also
  `UNDER_SPECIFIED_REFUSAL`.
- `FAIL`: the primary decision is wrong, a required value is out of tolerance,
  a critical/high error occurs, or an unsupported claim is asserted.
- `REFUSAL_CORRECT`: a refusal-required case is refused with the exact refusal
  class/reasons and all required blocked-claim, missing-information, and safe
  description fields, without a critical/high error.
- `REFUSAL_INCORRECT`: a refusal-required case is accepted, or an answerable
  case is refused, or the refusal class/decision is wrong. The corresponding
  over-refusal or false-acceptance error is recorded.

`REFUSAL_CORRECT` and `REFUSAL_INCORRECT` are task outcomes, not a replacement
for per-field scores. The benchmark report must retain case-level evidence,
task/family summaries, each critical/high error rate, refusal metrics,
comparability accuracy, causal-boundary violation rate, and optional
calibration. A single aggregate score cannot be used as V1 acceptance.

## 9 Contamination contract

### 9.1 Exclusion registry

Before any case is generated, the implementation must create or load
`PSE-V1-EXCLUSION-REGISTRY@1.0.0`. Each entry contains:

```text
ExclusionEntry {
    artifact_id
    source_id
    normalized_doi | null
    canonical_url | null
    source_family_id
    source_content_sha256 | null
    normalized_text_sha256 | null
    exact_shingle_digest
    fuzzy_fingerprint
    semantic_cluster_id | null
    benchmark_case_ids
    benchmark_case_hashes
    split_names
    exclusion_reason
}
```

The registry covers source documents, evidence excerpts, benchmark prompts,
structured answers, generated artifacts, rubrics, and manifests. It is
versioned and hash-bound. All V1 artifact IDs and fingerprints are excluded
from future training-corpus construction, including public-development cases.

### 9.2 Detection rules

The frozen contamination audit runs in this order:

1. **Exact identity:** normalize Unicode with NFKC, lowercase, normalize
   whitespace/line endings, and compute SHA-256 for full text, each paragraph,
   and each exact evidence span.
2. **Exact text overlap:** compute SHA-256 hashes of contiguous normalized
   13-token shingles and reject any unapproved overlap with the exclusion
   registry or a case in another split.
3. **Normalized/fuzzy overlap:** compute the deterministic normalized token
   5-gram Jaccard and normalized character 5-gram Jaccard. A candidate with
   either score at or above `0.85`, or normalized edit similarity at or above
   `0.90`, is a contamination review failure until explicitly adjudicated.
4. **Semantic duplication where feasible:** run the pinned semantic-duplicate
   detector declared by `contamination.detector_version` over candidate pairs.
   The detector is a screening mechanism, never a ground-truth judge. Every
   candidate above its registered threshold receives an expert disposition;
   an unresolved candidate cannot enter validation or hidden splits.
5. **Source-family isolation:** a source family, DOI family, provider export,
   protocol template, expert-author batch, or generator/mutation lineage may
   not cross a split boundary.

The normalization and detector versions, thresholds, candidate decisions, and
reviewer evidence are stored in the contamination audit manifest. A missing
detector or unresolved candidate is `BLOCKED`, not silently accepted.

### 9.3 Generated-seed isolation

Generators use disjoint namespaces:

```text
PSE-V1/PUBLIC_DEVELOPMENT/<generator>@<seed-block>
PSE-V1/FROZEN_VALIDATION/<generator>@<seed-block>
PSE-V1/HIDDEN_FINAL/<generator>@<seed-block>
```

Seed blocks and generator versions are recorded. A mutation records the parent
hash and cannot reuse a seed block from another split. Hidden seeds are not
available to the public generator or training/data pipeline.

### 9.4 Future training-corpus exclusion interface

RES-22 must consume a machine-readable exclusion manifest before corpus
construction. The interface is:

```text
PSE-V1-EXCLUSION-MANIFEST@1.0.0 {
    benchmark_version
    manifest_digest
    case_id/case_hash entries
    source/document/DOI/content fingerprints
    source_family and semantic-cluster exclusions
    generated-seed and mutation-lineage exclusions
    split metadata without hidden payloads
    exclusion_policy_version
}
```

Corpus construction must fail closed when the manifest digest is absent,
stale, or unverifiable. It must reject exact, normalized/fuzzy, and known
semantic-cluster matches before training data is materialized. The interface
identifies hidden artifacts by digest/ID without granting the training
workflow access to hidden payloads or answers.

## 10 Freeze and versioning policy

### 10.1 Bound artifacts and digests

The implementation must emit and record:

- `benchmark_semantic_version = PerformanceScience-Eval@1.0.0`;
- `case_schema_version = pse-case-schema@1.0.0`;
- `generator_version` and generator registry digest;
- `verifier_version` and scorer-profile digest;
- `split_manifest_version = split-manifest@1.0.0`;
- per-case canonical payload hashes;
- split membership hash;
- contamination/exclusion manifest hash; and
- benchmark manifest hash/digest over canonical sorted case, split, authority,
  scorer, and exclusion references.

The current RES-71 references consumed by V1 are:

```text
RES71_REFERENCE_INTERFACE_VERSION = res71-reference-interface@1.0.0
RES71_REFERENCE_CASE_DIGEST = sha256:d29d84699b7cf70c2d409d370c5ffd6c7ad7cd704375b14b541527a95fa385e5
RES71_VERIFIER_MANIFEST_DIGEST = sha256:9807b6e0be63abd44135bc855a97d37775ef09763d25c0f7025f4395ab673af6
RES71_SERIALIZATION_VERSION = 3
RES71_QUALIFIED_CONTENT_HEAD = 0a51127628f1bfc1f0b89064bf92d7fc2703ff39
```

The reference-case digest and qualification/verifier-manifest digest are
different artifacts and must not be substituted for one another.

### 10.2 Version bump rules

| Change | Required version action |
| --- | --- |
| Change to case meaning, expected answer, tolerance, authority precedence, refusal/error semantics, split isolation, contamination rule, or scorer result semantics | Major benchmark/schema bump; prior V1 is immutable and comparisons stop at the old manifest. |
| Additive capability/family, new optional structured field, new case set, new adversarial tag, or backward-compatible scorer profile that does not change existing results | Minor benchmark/schema bump and a new manifest; never mutate V1 in place. |
| Documentation wording, tooling fix, or metadata repair that preserves every case hash, split hash, authority digest, and score for existing cases | Patch bump; prove all preserved digests before release. |
| Change to generator/verifier/split/exclusion implementation that can change a case, membership, authority, or score | Corresponding major/minor release, never a patch-only change. |
| New hidden cases or changed hidden membership | New benchmark minor/major release according to semantic impact; old hidden set remains sealed. |

After `PerformanceScience-Eval@1.0.0` freezes, the following are immutable:
case payloads and IDs, expected answers, authority references, tolerance,
refusal/error taxonomy, split membership, source/seed isolation, case/split/
manifest hashes, and the scoring semantics used for published V1 results.

## 11 RES-71 integration contract

The next implementation mission must consume, not duplicate, the following
interfaces:

| RES-71 input | Required V1 integration |
| --- | --- |
| Registered operation inventory | Resolve `build_registered_operation_inventory()` and its validation result. Store operation ID, method version, family, disposition, input/output/provenance/refusal contracts, tolerance, and authority references in the case binding. Caller formulas, thresholds, operation IDs, or comparability overrides are rejected. |
| Unresolved-computation inventory | Resolve `build_unresolved_computation_inventory()`. Use its capability, disposition, expected refusal class, reason codes, safe description, and refusal path for negative/refusal cases. Deferred/represented/rejected work is a scored scientific outcome, not a hole to fill. |
| Reference cases | Consume `get_reference_case(s)`, `reference_case_manifest()`, and `reference_case_digest()` from `dynamislm.qualification`. Pin `res71-reference-interface@1.0.0` and the exact reference digest above. Later verifier code invokes the registered operation and compares its typed result/refusal; it does not calculate from the expected scalar itself. |
| Claim authority | Consume RES-70 `authorize_claim` and `validate_claim_authority`, exact claim intent/support/analysis/comparability/evidence hashes, allowed lower levels, and refusal result. A model proposal cannot mint an authorized claim. |
| Comparability authority | Consume RES-70 `assess_cross_source_comparability`, exact observation/provenance references, pairwise decision, material dimension findings, bridge execution, conditions, registry version/hash, and refusal. No label/unit/correlation/transitive shortcut is accepted. |
| Refusal taxonomy | Use the existing `RefusalClass`, `RefusalReasonCode`, `RefusalResult`, RES-70 reason codes, and RES-71 unresolved expected codes. Preserve blocked claim, missing information, safe descriptions, observation IDs, and evidence references. |
| Deterministic reference digest | Bind every engine-derived case to the exact reference/manifest digest and current qualified content head. If the digest or runtime validation differs, the case is stale and the benchmark generation/verification gate fails. |

The RES-71 reference adapter is a reference contract, not a second numerical
engine. No V1 code may add a parallel formula, alternate tolerance, hidden
unit conversion, semantic relabelling, or LM arithmetic fallback.

## 12 Acceptance matrix

The matrix below proves contract coverage for every claimed V1 capability. It
is a design matrix, not a claim that the full case inventory already exists.
The implementation manifest must contain at least one valid case for every
applicable cell in public development, frozen validation, and hidden final.

Legend: `SYN` = deterministic synthetic; `ENG` = deterministic engine-derived;
`EXP` = expert-authored semantic; `SRC` = source-backed extraction/evidence;
`MUT` = adversarial mutation. `A71` = RES-71 deterministic/reference
authority; `A70` = RES-70/RES-69/RES-60/RES-62 typed authority; `AE` = frozen
expert rubric; `AS` = source/exact-span authority; `COMP` = composite bindings.
Scorers are the Section 8 profiles. `D/V/H` means all three splits.

| Capability | Benchmark family | Case source | Answer authority | Scorer | Adversarial coverage | Critical/high-error mapping | Split coverage |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `C01` terminology/construct | `F01`, `F03`, `F11` | `EXP`, `SRC`, `MUT` | `AE`, `AS`, `A70` | `CLASSIFICATION`, `STRUCTURED`, `EVIDENCE` | Alias collision, same label/different construct, unsupported synonym | `FALSE_SCIENTIFIC_ACCEPTANCE`, `WRONG_MEASUREMENT_IDENTITY` | `D/V/H` |
| `C02` protocol extraction | `F02` | `EXP`, `SRC`, `MUT` | `AE`, `AS`, `A70` | `STRUCTURED`, `EVIDENCE`, `REFUSAL` | Missing device, threshold, event, phase, sampling, selection, aggregation | `WRONG_MEASUREMENT_IDENTITY`, `FALSE_SCIENTIFIC_ACCEPTANCE` | `D/V/H` |
| `C03` measurement identity | `F01`, `F03`, `F04`, `F13` | `ENG`, `EXP`, `SRC`, `MUT` | `A71`, `A70`, `AE`, `AS` | `CLASSIFICATION`, `STRUCTURED`, `COMPARABILITY` | Same label/different measurand, method/version collision, estimator relabel | `WRONG_MEASUREMENT_IDENTITY`, `FALSE_COMPARABILITY_ACCEPTANCE`, `DIRECT_DERIVED_COLLAPSE` | `D/V/H` |
| `C04` value origin/role | `F03`, `F05`, `F10`, `F13` | `ENG`, `EXP`, `SRC`, `MUT` | `A71`, `A70`, `AE`, `AS` | `CLASSIFICATION`, `STRUCTURED` | Provider-derived as direct, model estimate as measurement, latent/physiology inference | `DIRECT_DERIVED_COLLAPSE`, `UNSUPPORTED_LATENT_OR_PHYSIOLOGICAL_INFERENCE` | `D/V/H` |
| `C05` unit/normalization | `F03`, `F04`, `F06`, `F05` | `ENG`, `EXP`, `MUT` | `A71`, `A70`, `AE` | `NUMERIC`, `CLASSIFICATION`, `STRUCTURED` | Same unit/not same measurand, conversion without provenance, wrong denominator/scale | `INVENTED_NUMERICAL_SCIENCE`, `FALSE_COMPARABILITY_ACCEPTANCE`, `WRONG_MEASUREMENT_IDENTITY` | `D/V/H` |
| `C06` frame/sign/event/phase | `F02`, `F05`, `F06`, `F13` | `ENG`, `EXP`, `MUT` | `A71`, `A70`, `AE` | `CLASSIFICATION`, `STRUCTURED`, `REFUSAL` | Axis/frame/sign mismatch, event boundary, phase method, timebase, invalid domain | `INVENTED_NUMERICAL_SCIENCE`, `WRONG_MEASUREMENT_IDENTITY`, `FALSE_SCIENTIFIC_ACCEPTANCE` | `D/V/H` |
| `C07` comparability | `F04`, `F06`, `F13`, `F14` | `ENG`, `EXP`, `SRC`, `MUT` | `A71`, `A70`, `AS`, `COMP` | `COMPARABILITY`, `CLASSIFICATION`, `REFUSAL` | Same label, provider/device/protocol mismatch, bridge absent, non-transitivity, correlation/agreement | `FALSE_COMPARABILITY_ACCEPTANCE`, `FALSE_SCIENTIFIC_ACCEPTANCE`, `WRONG_MEASUREMENT_IDENTITY` | `D/V/H` |
| `C08` biomechanics calculation interpretation | `F05`, `F06`, `F13`, `F14` | `ENG`, `SYN`, `MUT` | `A71` plus `A70` refusal | `NUMERIC`, `STRUCTURED`, `REFUSAL` | Nonfinite/invalid input, unregistered RFD/MPV/power, wrong gravity/system mass, LM arithmetic | `INVENTED_NUMERICAL_SCIENCE`, `FALSE_SCIENTIFIC_ACCEPTANCE` | `D/V/H` |
| `C09` longitudinal change | `F07`, `F10`, `F12` | `ENG`, `EXP`, `MUT` | `A71`, `A70`, `AE` | `NUMERIC`, `STRUCTURED`, `CAUSAL_BOUNDARY` | Numerical vs comparable vs error-relative vs practical meaning; current-in-reference leakage | `FALSE_SCIENTIFIC_ACCEPTANCE`, `CAUSAL_OVERCLAIM`, `UNSUPPORTED_LATENT_OR_PHYSIOLOGICAL_INFERENCE` | `D/V/H` |
| `C10` analysis selection | `F08`, `F09`, `F13`, `F14` | `ENG`, `EXP`, `MUT` | `A71` operation disposition, `A70` analysis authority | `CLASSIFICATION`, `STRUCTURED`, `REFUSAL` | Deferred operation, wrong support shape, independence, missing design, wrong class | `WRONG_ANALYSIS_CLASS`, `FALSE_SCIENTIFIC_ACCEPTANCE` | `D/V/H` |
| `C11` within/between/group | `F08`, `F09`, `F07`, `F13` | `ENG`, `EXP`, `MUT` | `A70`, `A71` reference/refusal | `CLASSIFICATION`, `STRUCTURED`, `REFUSAL` | One-row-per-athlete as within, pseudoreplication, group-to-individual, no transitive level | `BETWEEN_TO_WITHIN_MISINFERENCE`, `WRONG_ANALYSIS_CLASS`, `CAUSAL_OVERCLAIM` | `D/V/H` |
| `C12` reliability/uncertainty/validity | `F07`, `F08`, `F10`, `F13` | `ENG`, `EXP`, `SRC`, `MUT` | `A71`, `A70`, `AE`, `AS` | `CLASSIFICATION`, `STRUCTURED`, `REFUSAL` | Reliability vs agreement, error vs meaningfulness, validity scope, unavailable SEM/MDC/CI | `FALSE_SCIENTIFIC_ACCEPTANCE`, `WRONG_ANALYSIS_CLASS`, `UNSUPPORTED_LATENT_OR_PHYSIOLOGICAL_INFERENCE` | `D/V/H` |
| `C13` evidence extraction | `F11`, `F01`, `F02` | `SRC`, `EXP`, `MUT` | `AS` with `AE` review | `EVIDENCE`, `STRUCTURED`, `CLASSIFICATION` | Span mismatch, source version mismatch, unsupported paraphrase, extraction vs interpretation | `FALSE_SCIENTIFIC_ACCEPTANCE`, `WRONG_MEASUREMENT_IDENTITY` | `D/V/H` |
| `C14` evidence applicability/scope | `F01`, `F04`, `F10`, `F11`, `F12` | `SRC`, `EXP`, `MUT` | `AS`, `A70`, `AE` | `CLASSIFICATION`, `STRUCTURED`, `REFUSAL` | Indirect evidence used as target prior, population mismatch, method-validity as applicability | `FALSE_SCIENTIFIC_ACCEPTANCE`, `FALSE_COMPARABILITY_ACCEPTANCE`, `UNSUPPORTED_LATENT_OR_PHYSIOLOGICAL_INFERENCE` | `D/V/H` |
| `C15` causal discipline | `F07`, `F09`, `F12`, `F13` | `EXP`, `SRC`, `MUT`, `ENG` | `A70`, `AE`, `A71` claim/refusal | `CAUSAL_BOUNDARY`, `CLASSIFICATION`, `REFUSAL` | Association-to-causation, temporal overclaim, mechanistic hypothesis relabel, prediction/causation | `CAUSAL_OVERCLAIM`, `FALSE_SCIENTIFIC_ACCEPTANCE`, `UNSUPPORTED_LATENT_OR_PHYSIOLOGICAL_INFERENCE` | `D/V/H` |
| `C16` deterministic-result interpretation | `F05`, `F07`, `F10`, `F14` | `ENG`, `MUT` | `A71` exact result/refusal and `A70` claim boundary | `NUMERIC`, `STRUCTURED`, `REFUSAL`, `CAUSAL_BOUNDARY` | Changed number, dropped provenance, result/refusal inversion, stronger claim than output | `INVENTED_NUMERICAL_SCIENCE`, `FALSE_SCIENTIFIC_ACCEPTANCE`, `CAUSAL_OVERCLAIM` | `D/V/H` |
| `C17` scientific error detection | `F13`, every affected family | `EXP`, `ENG`, `SRC`, `MUT` | `A71`, `A70`, `AE`, `AS` | `CLASSIFICATION`, `STRUCTURED`, `REFUSAL` | All critical/high classes, compound errors, safe partial correction | Every critical/high class listed in Section 5 | `D/V/H` |
| `C18` justified refusal | `F14`, all applicable families | `ENG`, `EXP`, `SRC`, `MUT` | `A71`, `A70`, `AS`, `AE` | `REFUSAL`, `STRUCTURED`, `CLASSIFICATION` | Missing metadata, unresolved identity, bridge, deferred operation, evidence, uncertainty, causal design, over-refusal | `FALSE_SCIENTIFIC_ACCEPTANCE`, `OVER_REFUSAL`, `UNDER_SPECIFIED_REFUSAL`, `EXCESSIVE_CONSERVATISM` | `D/V/H` |

Because every row has a family, source class, authority, scorer, adversarial
coverage, error mapping, and split rule, the V1 acceptance matrix is
`PASS` at the design level. Case generation must still fail closed if the
future manifest does not satisfy the row-level coverage constraints.

## 13 Implementation boundary for the next mission

The next authorized mission may implement only the frozen interfaces needed to
materialize this contract:

1. immutable case serialization and validation;
2. registered generator/origin adapters;
3. RES-71 reference/operation/refusal bindings;
4. expert semantic rubric ingestion and independent review metadata;
5. source/evidence reference and contamination audit records;
6. deterministic split allocation and manifest hashing;
7. task-level scorers and error-class reporting; and
8. hidden-store and training-exclusion preflight interfaces.

It may not alter scientific formulas, tolerances, operation identities,
historical hashes, RES-71 registries, refusal semantics, or claim authority.
It may not run a model, generate the full benchmark, train anything, or start
RES-22/RES-23. Any required change to a scientific authority is a separate
mission and blocks V1 implementation until requalified.

## 14 Freeze disposition

| Required freeze component | Disposition |
| --- | --- |
| Case schema | `FROZEN` |
| Capability coverage | `FROZEN` |
| Error taxonomy | `FROZEN` |
| Answer authority | `FROZEN` |
| Split governance | `FROZEN` |
| Case provenance | `FROZEN` |
| Scoring contract | `FROZEN` |
| Contamination contract | `FROZEN` |
| Versioning freeze | `FROZEN` |
| RES-71 integration | `FROZEN` |
| Acceptance matrix | `PASS` |

No claimed V1 capability lacks a benchmark family, case origin, authority,
scorer, adversarial contract, critical-error mapping, or split rule. Therefore
the design is not blocked. The full benchmark remains intentionally
unmaterialized until the next authorized RES-21 implementation mission.
